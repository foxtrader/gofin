package ta

import (
	"testing"
)

func TestDualThrust(t *testing.T) {
	/*k := newIndicatorTestKline()

	buy, sell := DualThrust(k.OpenValues(), k.LowValues(), k.HighValues(), k.CloseValues(), 20, 0.1, 0.2)
	if len(buy) != k.Len() || len(sell) != k.Len() {
		t.Errorf("buy/sell length error")
		return
	}
	fmt.Println(buy)
	fmt.Println(sell)*/
}
