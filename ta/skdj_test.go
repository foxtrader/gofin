package ta

import (
	"testing"
)

func TestSkdj(t *testing.T) {
	/*k, err := data.NewTestKlineMaotai()
	if err != nil {
		t.Error(err)
		return
	}
	begin := time.Date(2017, 05, 02, 0, 0, 0, 0, time.UTC)
	end := time.Date(2017, 06, 02, 0, 0, 0, 0, time.UTC)
	k = k.SliceBetweenEqual(begin, end)
	RSV, K, D := SKDJ(k.LowValues(), k.HighValues(), k.CloseValues(), 9, 3)

	fmt.Println(RSV)
	fmt.Println(K)
	fmt.Println(D)*/
}
